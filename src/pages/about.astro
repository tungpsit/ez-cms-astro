---
import SiteLayout from '../layouts/SiteLayout.astro';
import { getCollection, render } from 'astro:content';

const pages = await getCollection('pages');
const aboutPage = pages.find(page => page.id === 'about');

let Content = null;
if (aboutPage) {
  const rendered = await render(aboutPage);
  Content = rendered.Content;
}
---

<SiteLayout title="About">
  <div class="max-w-4xl mx-auto px-4 sm:px-6 py-12">
    <div class="mb-12 text-center">
      <h1 class="text-3xl md:text-4xl lg:text-5xl font-bold text-slate-100 mb-4">
        About <span class="gradient-text">EZ CMS</span>
      </h1>
      <p class="text-xl text-slate-400">
        A modern content management system for the modern web
      </p>
    </div>

    {Content ? (
      <div class="prose max-w-none">
        <Content />
      </div>
    ) : (
      <div class="space-y-12">
        <section class="card">
          <h2 class="text-2xl font-bold text-slate-100 mb-4">Our Mission</h2>
          <p class="text-slate-400 leading-relaxed">
            EZ CMS is built to make content management simple, fast, and enjoyable. 
            We believe that managing your website should be as easy as writing in a document, 
            without sacrificing performance or flexibility.
          </p>
        </section>

        <section class="grid grid-cols-1 md:grid-cols-2 gap-6">
          <div class="card">
            <div class="w-12 h-12 rounded-xl bg-emerald-500/10 flex items-center justify-center mb-4">
              <svg class="w-6 h-6 text-emerald-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 10V3L4 14h7v7l9-11h-7z" />
              </svg>
            </div>
            <h3 class="text-lg font-semibold text-slate-100 mb-2">Built on Astro</h3>
            <p class="text-slate-400 text-sm">
              Leveraging Astro's content layer for lightning-fast static site generation.
            </p>
          </div>

          <div class="card">
            <div class="w-12 h-12 rounded-xl bg-violet-500/10 flex items-center justify-center mb-4">
              <svg class="w-6 h-6 text-violet-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 20l4-16m4 4l4 4-4 4M6 16l-4-4 4-4" />
              </svg>
            </div>
            <h3 class="text-lg font-semibold text-slate-100 mb-2">Developer Friendly</h3>
            <p class="text-slate-400 text-sm">
              TypeScript, React, and modern tooling for a great developer experience.
            </p>
          </div>

          <div class="card">
            <div class="w-12 h-12 rounded-xl bg-cyan-500/10 flex items-center justify-center mb-4">
              <svg class="w-6 h-6 text-cyan-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M7 21a4 4 0 01-4-4V5a2 2 0 012-2h4a2 2 0 012 2v12a4 4 0 01-4 4zm0 0h12a2 2 0 002-2v-4a2 2 0 00-2-2h-2.343M11 7.343l1.657-1.657a2 2 0 012.828 0l2.829 2.829a2 2 0 010 2.828l-8.486 8.485M7 17h.01" />
              </svg>
            </div>
            <h3 class="text-lg font-semibold text-slate-100 mb-2">Customizable</h3>
            <p class="text-slate-400 text-sm">
              Themes and plugins let you tailor your site to your exact needs.
            </p>
          </div>

          <div class="card">
            <div class="w-12 h-12 rounded-xl bg-amber-500/10 flex items-center justify-center mb-4">
              <svg class="w-6 h-6 text-amber-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m5.618-4.016A11.955 11.955 0 0112 2.944a11.955 11.955 0 01-8.618 3.04A12.02 12.02 0 003 9c0 5.591 3.824 10.29 9 11.622 5.176-1.332 9-6.03 9-11.622 0-1.042-.133-2.052-.382-3.016z" />
              </svg>
            </div>
            <h3 class="text-lg font-semibold text-slate-100 mb-2">Secure</h3>
            <p class="text-slate-400 text-sm">
              Static output means fewer attack vectors and better security.
            </p>
          </div>
        </section>

        <section class="card text-center py-12">
          <h2 class="text-2xl font-bold text-slate-100 mb-4">Ready to Start?</h2>
          <p class="text-slate-400 mb-6">
            Access the admin panel and start creating content today.
          </p>
          <a href="/admin" class="btn btn-primary">
            Open Admin Panel
          </a>
        </section>
      </div>
    )}
  </div>
</SiteLayout>
